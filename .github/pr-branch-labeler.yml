# Salla company standard — mirrors SallaApp/.CI/pr-branch-labeler.
# Maps PR head-branch prefix → label (used for auto semantic versioning).
feature:
  - "feature/*"
bugfix:
  - "bugfix/*"
  - "fix/*"
hotfix:
  - "hotfix/*"
documentation:
  - "docs/*"
